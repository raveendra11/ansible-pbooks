---
collections:
  # Kubernetes management - used for namespaces, RBAC, deployments
  - name: kubernetes.core
    version: ">=2.4.0"

  # Google Cloud modules - used for GKE credential fetch
  - name: google.cloud
    version: ">=1.3.0"

  # Community general utilities
  - name: community.general
    version: ">=7.0.0"
